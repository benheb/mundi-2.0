<div class="row">
  <div class="col-xs-12 section-header">
    <h2>Favorites</h2>
    <ul>
      {{#each model as |item|}}
        <li>{{item.datasetId}}</li>
      {{/each}}
    </ul>
  </div>
</div>
